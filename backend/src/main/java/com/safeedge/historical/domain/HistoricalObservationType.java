package com.safeedge.historical.domain;

/**
 * Provenance of a static historical odds record. football-data.co.uk v1 quotes
 * are pre-match snapshots without a verified opening/closing timestamp.
 */
public enum HistoricalObservationType {
	PRE_MATCH_SNAPSHOT
}
